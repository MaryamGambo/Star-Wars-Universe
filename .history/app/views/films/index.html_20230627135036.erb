<div class="container">
  <p class="text-success"><%= notice %></p>

  <h1 class="display-4 mt-5">Films</h1>

  <table class="table table-hover mt-3">
    <thead>
      <tr>
        <th>ID</th>
        <th>Title</th>
        <th>Director</th>
        <th>Release Date</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @films.each do |film| %>
        <%= render partial: "film", locals: { film: film } %>
      <% end %>
    </tbody>
  </table>

  <%= link_to "New film", new_film_path, class: "btn btn-primary" %>
</div>
